
<%= simple_form_for @room do |form| %>
  <%= form.input :name %>
  <%= form.submit "Save", class: 'btn btn-success' %>
<%end%>
<style type="text/css">
	.room-nav-link {
    border: 1px solid #cce5ff;
    background: #00000;
}
</style>